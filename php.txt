1) В PHP в отличии от JS переменные за пределами функции, не доступны внутри функции. Т.е. нет замыкания. Все передается через аргументы. НО есть возможность сделать такое замыкание с помощью конструкции use https://metanit.com/php/tutorial/2.20.php

2) В PHP есть глобальные переменные: 
__DIR__ - полный путь к директории с к текущему файлу
__FILE__ - полный путь к текущему файлу

3) Конструкции php в шаблоне:
	1) Двоеточие и endif позволяет писать конструкцию в более читаемом виде.
 
	2) Эта конструкция позволяет быстро вывести переменную:
 
3) Стандартный вывод (не очень читаемо):
 
4) $array[] = $var; с помощью этого выражения в конец массива добавляется элемент. Если надо добавить сразу несколько элементов, то используется функция array_push

5) Область видимости функций (не переменных). Все функции в PHP имеют глобальную область видимости: 

6) С помощью конструкции &$someVariable можно менять значение по ссылке даже для примитивных типов (number/string):

7) static $someVariable = 0;
Это статическая переменная. Этот функционал позволяет реализовать замыкание как в JS. Подробнее тут: https://www.elated.com/php-variable-scope-all-you-need-to-know/ 

8) В php есть способ получать глобальные переменные в функции, без необходимости передавать аргумент. https://slusar.su/izuchaem-php-7-n5-rabota-s-funkciyami-oblast/ 

9) ob_start() и ob_end_clean() – позволяет сохранить все последовательные выводы (echo/print) в одну переменную

10) Переменные HTTP запроса можно получить с помощью: 1) специальных переменных $_GET $_POST $_COOKIE 2) специальной переменной $_REQUEST которая в себе содержит get/post/cookie 3) специальная функция filter_input, которая позволяет безопасно (без лишних проверок) получить get/post/cookie и при необходимости отфильтровать их

11) Про сессию в PHP подробно написано https://anton.shevchuk.name/php/php-for-beginners-session/
